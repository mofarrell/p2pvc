


/* initializes a UDP server, returns 0 on success or a corresponding errno
 * on failure */
int init_server

/* sample code which can hopefully write and read to another peer */
int main(int argc, char **argv) {
  char *testmesg = "hello";
  
  char *port = getenv("peerport\n");
  if



}
